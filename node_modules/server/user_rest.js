//user_rest.js

var dbapi = require('server/dbapi');
module.exports = {
    main : function (app) {
        app.get('/api/user', function (req, res) {
            res.send('user');
        });
        app.post('/api/user', function (req, res) {
            res.send('user');
        });
        app.get('/api/user/:id', function (req, res) {
            res.send('user');
        });
        var fnUserUpdate = function (req, res) {
        };
        app.post('/api/user/:id',fnUserUpdate);
        app.patch('/api/user/:id',fnUserUpdate);

        return app;
    }
};
